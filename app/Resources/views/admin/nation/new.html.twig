{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-xs-12">
                <h1>New Nation</h1>
                {{ form_start(nationForm) }}
                {{ form_widget(nationForm) }}
                <button type="submit" class="btn btn-primary">Create</button>
                {{ form_end(nationForm) }}
            </div>
        </div>
    </div>
{% endblock %}
