{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-xs-12">
                <h1>New Game</h1>
                {{ form_start(gameForm) }}
                {{ form_widget(gameForm) }}
                <button type="submit" class="btn btn-primary">Save</button>
                {{ form_end(gameForm) }}
            </div>
        </div>
    </div>
{% endblock %}
