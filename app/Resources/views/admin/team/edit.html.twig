{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-xs-12">
                <h1>Edit team</h1>
                {{ form_start(teamForm) }}
                    {{ form_row(teamForm.name) }}
                    {{ form_row(teamForm.nation) }}
                <button type="submit" class="btn btn-primary">Save</button>
                {{ form_end(teamForm) }}
            </div>
        </div>
    </div>
{% endblock %}
